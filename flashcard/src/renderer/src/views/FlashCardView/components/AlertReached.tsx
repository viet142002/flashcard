interface AlertReachedProps {
    onFetchNewList: () => void
}

export function AlertReached({ onFetchNewList }: AlertReachedProps) {

    return (
        <div>
            hello1
        </div>
    )
}